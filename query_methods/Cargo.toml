[package]
name = "query_methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = { version = "^2.1.2" }
#core = { path = "../core", features = ["guest"] }

[package.metadata.risc0]
methods = ["guest"]
